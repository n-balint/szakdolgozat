Source of the original parser:
https://github.com/shotover/tree-sitter-cql

Modified parts:
- Types are properly recognized into the different categories
- `WITH CLUSTERING ORDER BY` now handles multiple fields